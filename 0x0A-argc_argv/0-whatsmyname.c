#include <stdio.h>
/**
 * main - starting point
 * @argc: arg count
 *
 * @argv: array of pointers vector
 *
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%\n", argv[]);
	return (0);
}
